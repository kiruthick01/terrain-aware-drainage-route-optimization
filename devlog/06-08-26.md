# 2026-08-06

lost the dem file, had to redownload from opentopography again. same
srtm 30m data, saved to data/raw/chennai_velachery_dem.tif

qgis kept showing it as a flat gray blob, thought the file was broken.
turned out it was just a rendering issue (chennai's too flat, low
contrast by default). checked it directly with rasterio instead and
the data's actually fine - shape (187,250), elevation -6 to 44m, right
coords. stopped fighting qgis and moved on.

also switched from python 3.14 to 3.12 bc rasterio/gdal don't have
wheels for 3.14 yet. installed clean after that.

had claude code scaffold the repo structure, write scope.md, and build
the first pipeline script (terrain.py) - loads dem, reprojects to utm
44n (need meters not degrees for slope calc), computes slope + aspect
using whitebox (richdem wouldn't install on apple silicon).

output looks legit: cell size 30.3m (correct), slope mean 1.76°
(makes sense, velachery is basically flat which is why it floods),
aspect fine except flat cells return -1 instead of a real direction -
need to remember to skip those later or it'll mess up any averaging.

next: draw the zone boundary in qgis, then flow accumulation + rainfall
data pull